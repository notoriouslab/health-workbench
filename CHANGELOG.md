# Changelog

## 未發布（開發中）

### 顯示改版（六分頁）

- 分頁改為 總覽／就醫／用藥／檢驗／測量／睡眠呼吸。檢驗有自己的分頁
  （全部項目清單＋每項趨勢＋逐筆表，不再只能從總覽點入）；測量分頁分
  身體、循環、活動、其他四個子區塊，不再把十幾條線擠在同一頁
- 新增畫面：疫苗接種（就醫分頁）、運動記錄（測量分頁，依年摺疊）、
  行走穩定度、除脂體重、距離、爬樓層數、能量消耗。這些資料原本就有
  匯入，只是沒有畫面。各區塊沿用既有原則：沒有對應資料就不出現
  （例如沒戴手錶就不會有心率帶狀圖），不留空區塊
- 心率、血氧、呼吸速率改為「每日範圍與平均」帶狀圖（面為每日最低到
  最高、線為每日平均），心率另疊 Apple 的每日安靜心率；資料改讀每日
  彙總表，日後清理逐筆明細也不影響這些圖
- 睡眠呼吸分頁新增：Apple 睡眠時數（可展開各睡眠階段）、睡眠時數與
  CPAP 使用對照（附區間內使用時數佔睡眠時數的比例與樣本天數；兩來源
  對「一晚」的劃分不同，比例為期間合計、不逐晚相除）、睡眠期呼吸速率、
  Apple 血氧低點。只有 Apple 睡眠資料（沒有 CPAP）的使用者也看得到
  這個分頁
- 血壓圖加上居家量測參考線 130／80（2022 台灣高血壓治療指引，經國民
  健康署 722 原則頁轉述）、BMI 圖加上 18.5-24 參考帶（國民健康署），
  圖下標示來源與引用日期；只標參考位置，不做任何判定

### 釋放空間

- 「資料庫與匯入紀錄」卡新增「釋放空間」：刪除已彙總的逐筆明細並
  整理資料庫檔。確認畫面的筆數與容量全部當場實算；每日統計與所有
  圖表不受影響，但逐筆明細刪除後不會再回來，執行前會再次確認。
  刪除前程式會逐鍵核對彙總表已涵蓋將刪的明細，核對不過就不刪任何
  資料。整理空間需要約等於清理後資料庫大小的暫時磁碟空間

### 其他

- 匯入確認面板不再被其他區塊擠到只剩一行，判型結果、成員選擇與
  開始匯入鈕保持同時可見
- 版號旁新增「檢查新版」，點擊用瀏覽器開啟發佈頁；程式本身維持
  不連網、不自動檢查

- 新增 Apple 資料的每日彙總表：匯入時自動整理每日 × 型別 × 來源的統計
  （睡眠按階段識別字記每日分鐘數），既有資料庫開啟時自動升級並回填，
  升級前自動留存一份快照。資料量很大（數百萬筆）時，升級當次的開啟會
  多花一些時間，且快照需要與資料庫相當的可用磁碟空間。開啟程式時的活動趨勢（步數、距離、能量等
  六類）改讀彙總表，實測載入時間約為原本的一半以下（數十萬筆量級
  實測 0.75 至 1.3 秒 → 0.4 秒），且數值與原本逐筆計算完全一致
- 重新匯入涵蓋同一天的新匯出檔時，該日統計以全部資料重算（不會因為
  分批匯入而少算）；日後清理過舊明細後，若以只含部分來源的匯出檔重新
  匯入，完整的統計不會被覆蓋，並在品質摘要留下記號
- 誠實揭露：匯入時多了每日統計的整理，Apple 匯入時間比上一版增加
  約一成多（數十萬筆實測 11 秒級 → 13 秒級）。初版整理邏輯會多花
  約三分之一，已改為一次算好共用（對睡眠紀錄多的資料庫、以及舊庫
  升級時的一次性回填，改善更明顯）

- 修正 Apple 匯入 zip 時進度停在假的 100%：原本的百分比分母是壓縮檔大小、
  分子是解壓後的位元組，實測真實匯出檔壓縮比 24.1 倍，進度做到 4.9% 就顯示
  100%，之後大半時間停在「正在檢查檔案是否曾經匯入…（100%）」。改以解壓後
  的總量當分母，百分比全程單調、到 100% 即完成；總量不可得（超大 zip64 檔）
  時只顯示已處理筆數，不顯示不準的百分比
- Apple zip 匯入改為單遍：同一遍解壓內完成檔案比對、解析與入庫，不再為了
  比對而先整檔解壓一次。重複匯入同一顆 zip 從「整檔比對後跳過」變成秒級跳過
  （實測 0.14 秒）
- 匯入期間資料庫切換為 WAL 模式並於結束後收斂回單檔，匯入中強制關閉 App
  留下的暫存檔會在下次開啟時自動收斂，資料不受影響
- 資料庫 schema 升至 v5（來源紀錄新增 zip 容器指紋欄位），舊資料庫開啟時
  自動升級

## 0.7.6（2026-08-17）修正 EPUB 的閱讀器說明

- 上一版把 EPUB 說成「很多軟體與平台都支援」，對這份檔案並不成立：
  匯出檔的摺疊、搜尋與趨勢圖需要閱讀器會執行網頁程式，而多數閱讀器不會。
  實測 Google Play Books 不執行，只會顯示一行提示。App 內的匯出提醒、
  安裝包說明檔與 README 都改成實測過的說法
- 匯出檔在閱讀器不執行網頁程式時顯示的那行提示，補上替代路徑（改用
  Apple Books，或在電腦上改看匯出的單檔 HTML）
- 修正儲存對話框的預設檔名在 Windows 上會出現混合路徑分隔符
- 補上一批守衛：匯出流程（是否開對話框、取消時不寫檔、走哪個寫檔 API）、
  權限宣告不得放寬或遺漏、介面不得使用原生對話框、兩種匯出共用同一份
  檢視程式、含 CPAP 資料時 EPUB 仍合法、寫檔失敗要往上拋

## 0.7.5（2026-08-17）EPUB 匯出

- 新增「匯出 EPUB…」：與既有的單檔 HTML 匯出並存，兩者內容相同，共用
  同一份資料與同一份檢視程式，摺疊、搜尋與趨勢圖的互動在 EPUB 裡完全保留
  （Apple Books 於 Mac 與 iPhone 實機驗證）
- 手機、平板因此不必再另外安裝檢視器 App：iOS 無法用瀏覽器開啟本機 HTML，
  「檔案」App 的預覽也不執行網頁程式，EPUB 則用內建的 Books 直接開，
  字級可調
- 匯出前會顯示提醒：把檔案加進 Apple Books 後，只要 Books 的 iCloud 同步
  開著，這份含完整健康資料的檔案就會在 iCloud 有備份，並附關閉位置
- 產物體積遠小於單檔 HTML：同一份資料匯出，EPUB 約 111KB、HTML 約 627KB
- README 與安裝包說明檔改寫，兩種匯出格式的選擇與各自的限制寫在一起

## 0.7.0（2026-08-17）CPAP 睡眠呼吸與趨勢圖時間軸

### CPAP 睡眠呼吸資料匯入（Phase 1：ResMed 原生 EDF）

- 新增第三類資料來源：CPAP 呼吸器 SD 卡。拖入整張卡即可匯入，格式為公開
  標準 EDF（照規格自行實作，零依賴）。每晚摘要、呼吸事件與睡眠期血氧
  分別入庫，欄位以兩台機器共通的臨床量設計
- 新增「睡眠呼吸」分頁：每晚 AHI（可展開阻塞／中樞／低通氣分項）、
  使用時數、漏氣、送氣壓力、血氧與逐次事件表。趨勢頁另加一張每晚 AHI 圖，
  與體重、血壓共用同一時間區間可直接同期對照；總覽新增一張卡
- 匯入流程支援多檔來源：一次匯入整個資料夾，每個被解析的檔各留一筆來源
  紀錄，下次插卡只有新檔會被處理。判型只讀必要的檔，不影響既有來源的
  匯入速度
- 日期以「入睡當晚」為準（機器以正午為分界劃分一晚，與機器原生語意一致），
  未使用機器的日子不入庫也不畫成 0
- 沒有匯入 CPAP 資料時，分頁、總覽卡與趨勢圖都不會出現
- 資料庫 schema 升至版本 4。升級在單一交易內完成，並於升級前自動產生一份
  升級前的資料庫快照；快照失敗則中止升級

### 趨勢圖時間軸與區間選擇

- 趨勢圖時間軸改為時間比例：x 座標依實際日期在顯示區間內的位置定位，
  不再依「第幾個有資料的日期」等距排列。原本停止記錄的序列會畫在圖的
  最右端看似最新量測（生產庫實測：一條末筆距今數百天的序列畫在繪圖區
  右緣），只有 3 筆的檢驗項目也被鋪滿整個圖寬看似連續趨勢
- 趨勢頁新增時間區間選擇（近三月／近一年／全部），一組按鈕作用於全頁
  四張圖以維持同期對照；預設依資料新舊自動決定；單張圖在該區間內無
  資料時顯示提示與「看全部」入口
- x 軸刻度改依時間挑選，粒度階梯單調由細到粗（雙週／月／季／半年／
  年／每 2、5、10、20、50 年）逐級下降至不超過上限，避免改為時間定位
  後標籤重疊；跨度極大時至少保留首末兩個刻度，不會整條標籤消失
- 各序列名稱與最新值改標於繪圖區右側固定位置（原本貼在折線末端，
  會被右邊界截斷，或在停止記錄的序列上壓住其他資料）
- 密集序列的標記依區間內點數兩段降級，超過門檻只畫折線不畫標記
  （真實資料實測 SVG 節點降幅約九成）；健保成健的獨立標記不受影響。
  代價如實揭露：不畫標記的序列同時失去滑過看數值的提示，且原本
  238 至 400 點之間有完整標記的序列會改為較小標記或無標記
- 總覽頁的體重趨勢卡不套用「不畫標記」門檻（該卡沒有區間可切，
  否則使用者無法把逐點數值提示要回來）；其標記半徑由 3 降為 1.5
- 總覽頁血壓卡在單位旁顯示最近量測日期：原本以大字顯示數值卻不顯示
  日期，陳舊數值同樣會被誤讀成當前狀態
- 步數圖粒度隨區間變化（近三月逐日、其餘月平均）
- 日期無法識別的資料點（如 null）在繪製前剔除並於圖說標示筆數；
  原本一筆 null 就會把時間軸下界拉到 1970 且不會報錯
- `generated_at` 改用本機日期：原本 App 端用 UTC 日期、Python CLI 用
  本機日期，台北時區每天有八小時兩邊差一日；時間軸以它為上界後這個
  落差會直接反映在圖上

### 檢視層與匯入紀錄調整

- 睡眠呼吸分頁的用語改為只描述機器輸出：「治療壓力」改為「**送氣壓力**」，
  日期說明改為「機器以正午為分界劃分一晚」（仍標明日期語意為入睡當晚）。
  這兩個詞也納入非結論式用語守衛，之後寫回去會被建置檢查擋下
- 睡眠呼吸分頁新增「**每晚事件數**」圖（阻塞／中樞／低通氣／未分類各一條線）。
  它是聚合視角，不受逐筆保留範圍影響，資料累積多年都畫得完
- 逐筆事件改為「**年 → 晚 → 逐筆**」三層，預設全收，展開才載入該層內容。
  原本是一次列出最近 300 筆，量大時那是一個無法定位又可能不完整的切片；
  現在畫面上常駐的只有年份那幾行，十年就是十行
- 逐筆事件的保留範圍改為「**最近 365 晚的完整逐筆**」（原為「最近 2000 筆」）。
  按筆數切會落在某一晚的中間，那晚只剩一半事件而畫面上看不出被截斷；
  現在任一晚要嘛完整、要嘛不在檔案裡，並在畫面標明保留範圍
- **就醫時間軸依年分組**，同時只展開一年；從搜尋或用藥頁跳轉時會自動開在
  目標所在的年份
- **匯入紀錄同一批摺疊為一列**（顯示「N 個檔案」與該批合計，可展開看逐檔）。
  一次拖入 CPAP 記錄卡會產生數十筆來源紀錄，原本逐列呈現使該區塊無法閱讀
- 刪除與改歸屬**支援整批**：批次列的操作對整批生效，單一交易全成功或全回復；
  展開後仍可單獨處理某一個檔案
- 匯入時同一批檔案共用同一個匯入時刻。原本每筆各自取當下時間，批次大或機器
  慢就會跨秒，使同一批在畫面上被拆成數批
- 匯入頁說明卡顯示**版本與執行來源**（開發版／本機建置）。安裝版與開發版
  共用同一個資料目錄且版號相同，開錯版本時症狀會像功能故障
- 分頁「匯入資料」改名為「**資料管理**」：該分頁早已不只匯入，還有資料庫與
  匯入紀錄（含刪除、改歸屬）與成員管理入口
- 「匯出資料庫檔」與「匯入既有資料庫檔」自管理成員面板的進階區移到「資料
  管理」分頁最下方，成為「換電腦或備份」區塊。順序為匯出在前（安全、常用）、
  匯入在後並明示**會取代目前的資料庫**
- 「資料庫與匯入紀錄」與「換電腦或備份」改為可摺疊，預設收起；紀錄卡收起時
  仍顯示來源檔案筆數。資料管理分頁改為整頁捲動，區塊不再互相壓縮——原本每個
  區塊各自內部捲動，內容一多就被擠成很小的捲動區而難以操作

### 缺陷修正

- 修正：Apple 健康匯出資料夾若含 `._` 開頭的伴隨檔（在 FAT32／exFAT
  隨身碟或從 Windows 拷來的資料夾很常見），匯入會失敗。這類檔名會排在
  真正的匯出檔之前而被選中，讀取時遭作業系統權限層拒絕
- 修正：「資料庫與匯入紀錄」的「全部資料」統計漏算 Apple 體能訓練、
  身體數值與癌症篩檢三類，畫面上看不到這些資料的筆數
- 修正：**CPAP 來源檔案無法刪除**（操作失敗於資料庫外鍵限制）。刪除來源檔
  時漏了 CPAP 的三張表，而它們指向該來源紀錄
- 修正：**CPAP 來源檔案改歸屬時資料留在原成員**，而畫面回報「搬移 0 筆」
  看起來像成功。改歸屬的搬移範圍漏了 CPAP 三表
- 修正：**含 CPAP 資料的成員無法刪除**（同樣的外鍵限制）。成員刪除的連帶
  清除範圍漏了 CPAP 三表
- 修正：只有 CPAP 資料的成員，資料檢視整頁空白。判定「這位成員有沒有資料」
  時只查了就醫與 Apple 紀錄兩張表
- 修正：健保用藥品項檔快取在某些情況取不到時，西醫藥品會全部落到「診療項目
  與其他」分類，畫面只在說明行寫「未建快取」而沒有其他提示。改為必要時自動
  複製一份到資料目錄，取不到時把原因寫進主控台
- 修正：**品質旗標統計漏掉四張表**，匯入完成卡因此顯示「品質旗標：無」，
  看起來像這批資料沒有任何需要留意的地方。漏的是 CPAP 三張表（例如一晚
  中途取下面罩再戴回的 `multi_session`）與 Apple 體能訓練。品質報告的
  日期範圍同樣漏了 CPAP 三張表
- 收斂應用程式的檔案權限：移除對系統暫存目錄的寫入與刪除授權（開發期間
  留下的，程式並未使用）。匯出 HTML 與資料庫檔寫的是儲存對話框選定的路徑，
  由系統在選檔當下授權，不受影響
- 修正：**CPAP 匯入紀錄顯示的新增筆數是實際的近兩倍**。一次匯入的整批合計
  被記在 `STR.edf` 那一筆來源紀錄上，而匯入紀錄把同一批各筆相加，等於把整批
  又算了一次（檔案越多差距越大，實測接近兩倍）；展開批次時
  `STR.edf` 那行也掛著它自己沒有的事件數。改為每筆來源紀錄只記自己新增與
  略過了什麼，整批合計只用於匯入當下的報告卡。資料本身一直是正確的，
  錯的只有這個數字

## 0.5.0（2026-08-11）誤歸屬救援與備份

- 誤歸屬救援（匯錯成員的資料救得回來，原「刪整個成員重建」的唯一
  永久遺失路徑落幕）：匯入紀錄卡每筆來源檔案可「改歸屬」（連同全部
  資料列搬給正確成員；與目標既有紀錄重複者自動合併）或「刪除」
  （連帶該檔全部資料列，檔案可重新匯入）；操作前明細預覽＋確認，
  單一交易、中斷整批回滾
- 同人多檔重疊防護：刪除／改歸屬可能波及其他檔案也含有的紀錄時，
  預覽顯示警告（健保滾動視窗與 Apple 累積匯出天然重疊）
- 健保身分綁定守恆：健保檔只能改歸屬給未綁定身分證的成員；來源
  成員失去最後一份健保檔時自動解綁（改歸屬同時轉綁目標），誤綁
  的身分證不再卡住正主
- 匯出資料庫檔：管理成員面板進階區一鍵備份整庫（一致性快照、
  預設檔名含日期、同名拒絕），新電腦用「匯入既有資料庫檔」接回
- 仿單等外部連結修復：App 內點擊改開系統瀏覽器（原 target=_blank
  被 WebView 靜默攔下）；說明卡 GitHub 連結由「複製」改直接開啟
- 紅隊護欄擴充：救援操作中途失敗兩情境進 CI 對抗矩陣（全庫逐位元
  組不變斷言）；HTML 轉義一致性守衛（涵蓋屬性位置注入面）；
  node:test 128 項全綠
- 修正：只有單一來源資料（僅健保或僅 Apple）時，趨勢頁折線圖對
  空序列拋錯導致整個檢視空白（含匯出 HTML）；補檢視器錯誤邊界，
  單一分頁故障不再拖垮全部分頁；新增單一來源全分頁渲染回歸測試
- 匯出 HTML 的載入占位文字改為說明「這個開啟方式不執行網頁程式」並
  指到 iOS 檢視器 App（原文字讓人誤判成壞檔）
- 匯出 HTML 手機版面：窄螢幕總覽單欄、寬表格各自橫向捲動不再撐破
  整頁（診間用手機看的情境）；觸控目標維持 44px
- macOS DMG 內附「使用說明（請先閱讀）.txt」：放行、匯入、匯出與
  iOS 檢視器指引，不必先讀 GitHub 才會用；產出後掛載斷言說明檔與
  App 都在，同一支腳本在每次推 main 的 CI 就實跑
- README：補 iPhone／iPad 開啟匯出檔的方式（iOS 無法用 Safari 直接
  開本機 HTML）、四個分頁的畫面截圖、核心特色與參與開發指引
- DMG 說明檔圖示明確配位：原本交給 Finder 自動配位會落到視窗外
  （實測使用者打開 DMG 沒看到），改為指定座標並放大視窗，產出後
  斷言座標存在且落在可見範圍
- Windows 使用說明（SmartScreen 放行、%APPDATA% 資料位置）以獨立
  資產掛在 release 下載頁（安裝檔是單一執行檔，沒有可放說明的容器）
- 新增 `scripts/gen_demo_data.mjs`：產生合成示範資料與示範頁面
  （決定性輸出，README 截圖即由它產生；無真實個資，也可直接試用）
- 零 schema 變更；Python CLI 維持凍結

## 0.4.0（2026-08-10）多人資料管理

- 成員管理：新增／改名／刪除（刪除連帶該成員全部資料，輸入名稱二次
  確認；刪除後原始檔可重新匯入給其他成員）
- 匯入歸屬：每次明確選擇成員（無預設、可就地新增）；健保檔遮罩身分證
  作為驗證護欄（不符或已屬他人即阻擋零寫入），Apple 檔由面板顯著
  顯示歸屬供確認
- 依人檢視：header 成員切換器（跨啟動記憶），四分頁、搜尋、狀態列
  跟隨當前成員；儀表板標題帶成員名；匯入紀錄卡維持全庫、依成員分組
- 匯出單檔 HTML＝僅當前成員，檔名含成員名，任何裝置瀏覽器可開
- 匯入非破壞性紅隊護欄進 CI：七情境對抗矩陣（中途故障／歸戶不符／
  跨成員重複／畸形檔等），既有資料逐位元組不變、白名單斷言＋常駐
  負向自檢
- 介面打磨（走查回饋四輪）：匯入頁頂部說明卡（含 GitHub 連結）、
  通用選檔鈕、對話框記憶上次目錄、Apple 資料夾自動下潛一層、
  「匯入既有資料庫檔」降級收進管理面板進階區、暫時提示獨立通知列
- 零 schema 變更：既有資料庫直接沿用；Python CLI 維持凍結（單人
  oracle 與開發者路徑）

## 0.3.0（2026-08-10）Tauri 桌面 App（產品化）

- 桌面 App（macOS/Windows）：拖放或選檔匯入、自動判型、進度與結果
  報告卡、匯入/檢視雙分頁、資料庫與匯入紀錄卡、單檔 HTML 匯出
- 匯入引擎 JS 重寫並以差分對帳驗收：與 Python 管線對真實資料全表
  全等（數十萬筆 Apple＋健保全節區）；新增健保 XML 匯入（與 JSON
  跨格式對帳，白名單僅 r8 換行語意，真檔 7/7 驗證）
- 差分護欄實戰戰果：抓到並修復串流掃描 chunk 邊界丟紀錄 bug、
  tauri-plugin-sql 連線池幽靈讀（棄用改自有 rusqlite 單連線橋）
- 防護沿用並前置到介面：重複檔自動跳過、歸戶確認整合進匯入確認面板、
  不同人檔案阻擋零寫入、部分失敗續行明細
- CI：GitHub Actions 測試＋守衛＋雙平台建置（產物未簽章；Windows
  實機冒煙與簽章列分發前 backlog）
- Python CLI 凍結新功能，轉為開發者路徑與差分驗收 oracle

## 0.2.0（2026-08-09）設計改版與 UX 迭代

- 視覺改版「健康摘要卡」：洞察式總覽（最新值＋變化量）、Apple 健康風卡片、深淺色
- 時間軸篩選連動；用藥三分類（藥品/中醫用藥/診療項目）＋處方時間軸展開
- 藥↔看診雙向跳轉、搜尋結果全面可點選並捲動定位
- 匯入紀錄：每次匯入的時間/檔案/新增統計（schema v3＋前向遷移機制）
- 韌性：單筆壞紀錄續行不中止整批（parse_errors 進品質報告）
- bin/hwb 啟動器（任意目錄可執行）
- 審計：Jenny 規格符合度（5 發現全修）＋Karen 現實檢核（1 發現已修），
  紀錄於 docs/verification/

## 0.1.0（2026-08-08）MVP 核心四件套

- `hwb` CLI：import（自動判型、冪等）/ rebuild / status / quality / knowledge update
- 健保存摺醫療類 JSON adapter：14 節區、藥局調劑回退、醫囑對帳、遮罩身分證歸戶
- Apple Health adapter：串流解析、來源別單位修正、epoch/離群旗標、檔內去重
- 跨批次累加合併：內容指紋（D1）、視窗接續、疑似改版偵測、步數防雙計
- knowledge：labs.yaml 40 條（附來源與引用日期）、健保藥品品項快取 join、
  非結論式用語建置檢查
- 單檔互動 dashboard：總覽/時間軸/用藥/趨勢＋全文搜尋，離線可用、
  深淺色、<10MB 閘門、繁體中文
