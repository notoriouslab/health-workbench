{
  "name": "health-workbench-app",
  "private": true,
  "version": "0.9.0",
  "type": "module",
  "scripts": {
    "tauri": "tauri",
    "test": "node --test 'tests/**/*.test.mjs'"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}